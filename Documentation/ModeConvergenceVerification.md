# Mode-convergence and variable-count verification

- Base: InternalModes `9ff1b6789a1dd1978736eb86760135909bf9999d` (released beta.3). This increment adds an ordinary prepared-data convergence function and scalar/vector counts in the existing bulk solve; it introduces no new classes or replacement modes.
- Full authoring V2 suite: 373/373 passed. Subsequent WVM one-mode integration exposed a MATLAB scalar `repelem` orientation bug in the new measurement table; the comparison now explicitly returns column-oriented rows, with a new one-mode regression.
- Final clean exported MPM installation: **374/374 V2 cases passed**, zero failed/incomplete, on MATLAB R2025b Update 4 / Apple Silicon. The installed candidate includes the one-mode correction. A fresh preference directory and add-on installation folder isolated it from authoring code. Runtime provider symbols resolved inside that installation. This qualifies an unreleased candidate export; it does not claim a new provider release has been published.
- WVM advisory migration: all 18 source/product/advisory cases passed using the new shared comparison. The original beta.3 reference, equality tolerance and historical calibration files are unchanged. The added output-page breakdown reproduces the global errors/product counts under its explicitly common-prefix scope.
- The runnable figure example passed and was visually inspected. Its default case has 41 distinct positive Fourier radii: all 24 candidate waves meet the stated refinement tolerance, while the 25-point vertical grid supports 13 to 8 modes at the stated 1% G Gram tolerance. Inertial evidence remains separate. Results disclose the candidate ceiling and do not claim quadratic certification.
- The documentation builder ran once after the coherent source/documentation batch; only the bulk-solve API page and generated version history changed. No provider `docs:check` task exists. Canonical changelog equality, generated bulk-count API text and whitespace were checked. Analyzer reports no findings in the changed MATLAB implementation, example and tests, including the scalar-label correction.
- The final example evidence records a committed generator revision separately from the later artifact commit. Release/export and WVM dependency publication remain ordered integration steps after review; existing OceanKit snapshots have not been edited.
